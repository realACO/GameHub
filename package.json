{
  "name": "gamehub-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "npm --prefix frontend start",
    "build": "npm run frontend:build",
    "build:all": "npm run frontend:build",
    "build:frontend": "npm run frontend:build",
    "test": "npm --prefix frontend run test -- --watchAll=false --passWithNoTests",
    "frontend:start": "npm --prefix frontend start",
    "frontend:build": "npm --prefix frontend run build",
    "frontend:test": "npm --prefix frontend run test -- --watchAll=false --passWithNoTests"
  }
}
